name: Keep Render Service Alive

on:
  schedule:
    - cron: "2,12,22,32,42,52 * * * *"
  workflow_dispatch:

jobs:
  keep-alive:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Render service
        run: |
          curl -f --max-time 30 \
          https://academic-assistant-zsyy.onrender.com/api/health